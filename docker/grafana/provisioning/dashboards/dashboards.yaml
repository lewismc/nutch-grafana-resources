# Grafana Dashboard Provisioning for Apache Nutch Monitoring Stack
# Dashboards are provisioned via OpenTofu, not file provisioning
# https://grafana.com/docs/grafana/latest/administration/provisioning/#dashboards
#
# To provision dashboards locally via OpenTofu:
#   cd tofu
#   tofu init
#   tofu apply -var="grafana_url=http://localhost:3000" -var="grafana_auth=admin:admin"

apiVersion: 1

providers: []
